version: 2

sources:
  - name: raw
    description: Raw source tables loaded from Snowflake

    database: DBT_DEMO
    schema: RAW

    tables:
      - name: RAW_CUSTOMERS

      - name: RAW_PRODUCTS

      - name: RAW_ORDERS